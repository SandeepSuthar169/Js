console.log(x);
let x = 10   // Cannot access 'x' before initialization



// 1/ Globel execution context

// 1 Hoisting :->  define variable (let and const) before to access. 
// 2 The temporal dead zone (TDZ) :-> let and const create temporal dead zone, any variable who that delear in let and const, verialbel before access tham and throw error that is cannot access variable before initialization

